{
  "configurations": {
    "attach": {
      "adapter": "vscode-node",  // <- Add a comma here
      "default": true,
      "breakpoints": {
        "exception": {
          "all": "N",
          "uncaught": "N"
        }
      },
      "configuration": {
        "name": "Attach to Process",
        "type": "node",
        "request": "attach",
        "skipFiles": ["<node_internals>/**/*.js"],
        "processId": "${processId}"
      }
    }
  }
}
